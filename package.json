{
  "name": "world-proof-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/gateway.js"
  },
  "dependencies": {
    "@worldcoin/idkit-core": "^4.2.3",
    "pg": "^8.16.3",
    "viem": "^2.55.11"
  }
}
